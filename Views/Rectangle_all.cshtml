<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <title>All Rectangles</title>
</head>

<body>
    <div class="container">
        <h1>All your rectangle so far</h1>
        <ul>
          @foreach (var shape in @Model) {
            <li>First side is @shape.GetLength() and second side is @shape.GetWidth(). @if(@Model.IsSquare()){and it is a square } else { and it is a rectangle}<li>
          }
        </ul>
        <p><a href="/">Try another one</a></p>
        <form action="/rectangle_cleared" method="post">
          <button type="submit">Clear all results so far</button>
        </form>
    </div>
</body>

</html>
